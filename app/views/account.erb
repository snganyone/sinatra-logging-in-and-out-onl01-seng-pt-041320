<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  <% if Helpers.is_logged_in?(session)%>
  <h3>Hello, <%= Helpers.current_user(session).name %></h3>
  <h3>Your Current Balance is: <%= Helpers.current_user(session).balance %></h3>
  <a href="/logout">Log Out</a>

  <% else %>

  Please <a href="/">Log In</a> to view your current balance.

  <% end %>
    
  </body>
</html>