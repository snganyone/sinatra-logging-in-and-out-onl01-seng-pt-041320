<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  <% if Helpers.is_logged_in?(session) %>
    <h3>Welcome <%= Helpers.current_user(session).username %></h3>
    <h3>Your Current Balance is: <%= Helpers.current_user(session).balance %></h3>
    <a href="/logout">Log Out</a>
  <% else %>
    You Must <a href="/">Log In</a> to View Your Balance.
  <% end %>
    
  </body>
</html>