<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  <% @help = Helpers.new %>
  <% if @help.is_logged_in?(session)%>
  <h3>Hello, <%= @help.current_user(session).name %></h3>
  <h3>Your Current Balance is: <%= @help.current_user(session).balance %></h3>
  <a href="/logout">Log Out</a>
  <% else %>

  <% end %>
    

  </body>
</html>